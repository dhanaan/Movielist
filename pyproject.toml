[project]
name = "movielist"
version = "0.1.0"
description = "lightweight CLI tool to search movies/tv-series"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "requests>=2.34.2",
]
